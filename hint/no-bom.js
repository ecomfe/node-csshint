/**
 * @file no-bom 检测
 * @author ielgnaw(wuji0223@gmail.com)
 */

/**
 * 模块的输出接口
 *
 * @param {string} bufContent 当前检测文件 buffer 内容
 * @param {Array.<Object>} invalidList 不合法文件集合
 */
module.exports = function (bufContent, invalidList) {
    // var hasBOM = fileContent[0] === 0xEF
        // && fileContent[1] === 0xBB
        // && fileContent[2] === 0xBF;

    // console.log(hasBOM);

    // console.log(bufContent);
};
